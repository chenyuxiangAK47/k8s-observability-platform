name: CI/CD Pipeline

# 触发条件：当代码推送到 main/master 分支或创建 PR 时运行
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

# 工作流权限
permissions:
  contents: read
  checks: write

jobs:
  # Job 1: 验证 Kubernetes YAML 文件
  validate-k8s-yaml:
    name: Validate Kubernetes YAML
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Validate YAML syntax
        run: |
          echo "🔍 验证 Kubernetes YAML 文件语法..."
          
          # 查找所有 YAML 文件（排除 node_modules 等）
          find k8s -name "*.yaml" -o -name "*.yml" | while read file; do
            echo "检查: $file"
            # 使用 kubectl 验证 YAML 语法（dry-run）
            kubectl apply --dry-run=client -f "$file" || {
              echo "❌ $file 验证失败"
              exit 1
            }
          done
          
          echo "✅ 所有 Kubernetes YAML 文件验证通过"

  # Job 2: 验证 Helm Charts
  validate-helm-charts:
    name: Validate Helm Charts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Helm
        uses: azure/setup-helm@v3
        with:
          version: 'latest'

      - name: Add Helm repositories
        run: |
          helm repo add grafana https://grafana.github.io/helm-charts
          helm repo add jaegertracing https://jaegertracing.github.io/helm-charts
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
          helm repo update

      - name: Lint observability-platform chart
        run: |
          echo "🔍 验证 observability-platform Helm Chart..."
          cd helm/observability-platform
          helm dependency update
          helm lint .
          echo "✅ observability-platform Chart 验证通过"

      - name: Template observability-platform chart
        run: |
          echo "🔍 渲染 observability-platform Helm Chart 模板..."
          cd helm/observability-platform
          helm template observability-platform . --debug --dry-run > /dev/null || {
            echo "❌ observability-platform Chart 模板渲染失败"
            exit 1
          }
          echo "✅ observability-platform Chart 模板渲染成功"

      - name: Lint microservices chart
        run: |
          echo "🔍 验证 microservices Helm Chart..."
          cd helm/microservices
          helm lint .
          echo "✅ microservices Chart 验证通过"

      - name: Template microservices chart
        run: |
          echo "🔍 渲染 microservices Helm Chart 模板..."
          cd helm/microservices
          helm template microservices . --debug --dry-run > /dev/null || {
            echo "❌ microservices Chart 模板渲染失败"
            exit 1
          }
          echo "✅ microservices Chart 模板渲染成功"

  # Job 3: 验证 Python 代码（可选）
  validate-python:
    name: Validate Python Code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          # 检查每个服务的 requirements.txt 是否存在
          for service in user-service product-service order-service; do
            if [ -f "services/$service/requirements.txt" ]; then
              echo "安装 $service 依赖..."
              pip install -r services/$service/requirements.txt
            fi
          done
          # 安装代码检查工具
          pip install flake8 pylint

      - name: Lint Python code
        run: |
          echo "🔍 检查 Python 代码风格..."
          # 使用 flake8 检查代码风格（忽略一些常见警告）
          find services -name "*.py" -exec flake8 {} \; --max-line-length=120 --ignore=E501,W503 || {
            echo "⚠️  代码风格检查发现问题，但不阻止构建"
          }
          echo "✅ Python 代码检查完成"

      - name: Validate Python syntax
        run: |
          echo "🔍 验证 Python 语法..."
          find services -name "*.py" -exec python -m py_compile {} \; || {
            echo "❌ Python 语法错误"
            exit 1
          }
          echo "✅ 所有 Python 文件语法正确"

  # Job 4: 验证 Dockerfile（可选）
  validate-dockerfile:
    name: Validate Dockerfiles
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate Dockerfiles
        run: |
          echo "🔍 验证 Dockerfile 语法..."
          for dockerfile in services/*/Dockerfile; do
            if [ -f "$dockerfile" ]; then
              echo "检查: $dockerfile"
              # 使用 docker buildx 验证 Dockerfile（不实际构建）
              docker buildx build --dry-run -f "$dockerfile" "$(dirname $dockerfile)" || {
                echo "❌ $dockerfile 验证失败"
                exit 1
              }
            fi
          done
          echo "✅ 所有 Dockerfile 验证通过"

  # Job 5: 检查文档链接（可选）
  check-docs:
    name: Check Documentation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Markdown files
        run: |
          echo "🔍 检查 Markdown 文档..."
          # 检查是否有明显的 Markdown 语法错误（简单检查）
          find . -name "*.md" -not -path "./.git/*" -exec grep -l "^# " {} \; | head -5
          echo "✅ 文档检查完成"

  # Job 6: 总结报告
  ci-summary:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [validate-k8s-yaml, validate-helm-charts, validate-python, validate-dockerfile]
    if: always()
    steps:
      - name: CI Summary
        run: |
          echo "## 🎉 CI/CD 检查完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 检查项 | 状态 |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| Kubernetes YAML | ${{ needs.validate-k8s-yaml.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Helm Charts | ${{ needs.validate-helm-charts.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Python Code | ${{ needs.validate-python.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Dockerfiles | ${{ needs.validate-dockerfile.result }} |" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ needs.validate-k8s-yaml.result }}" != "success" ] || \
             [ "${{ needs.validate-helm-charts.result }}" != "success" ] || \
             [ "${{ needs.validate-python.result }}" != "success" ] || \
             [ "${{ needs.validate-dockerfile.result }}" != "success" ]; then
            echo "❌ 部分检查失败，请查看详细日志" >> $GITHUB_STEP_SUMMARY
            exit 1
          else
            echo "✅ 所有检查通过！" >> $GITHUB_STEP_SUMMARY
          fi

